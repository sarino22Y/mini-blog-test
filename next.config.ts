/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com', 'tse2.mm.bing.net', 'tse4.mm.bing.net', 'tse1.mm.bing.net'],
  },
};

module.exports = nextConfig;
